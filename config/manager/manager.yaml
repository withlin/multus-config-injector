apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: yce-multus-manager
  name: system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: yce-multus-manager
  namespace: kube-system
  labels:
    control-plane: yce-multus-manager
spec:
  selector:
    matchLabels:
      control-plane: yce-multus-manager
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: yce-multus-manager
    spec:
      containers:
      - command:
        - /manager
        args:
        - --enable-leader-election
        image: controller:latest
        name: manager
        resources:
          limits:
            cpu: 100m
            memory: 30Mi
          requests:
            cpu: 100m
            memory: 20Mi
      terminationGracePeriodSeconds: 10
